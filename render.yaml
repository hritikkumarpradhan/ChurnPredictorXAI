services:
  - type: web
    name: churn-predictor-xai
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --timeout 200
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.12
